---
description: Vite <-> React application component
labels: ['vite-app', 'app']
---

Import the app component:

```bash
bit import aps.design/examples/vite-app
```

### Run the application

You can run your application on a separate port to see it outside of the Bit workspace

```bash
bit run vite-app
```
