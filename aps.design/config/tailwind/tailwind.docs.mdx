---
description: Basic Tailwind config for a Bit project
tags: ['tailwind', 'css', 'module', 'preset']
---

This component is a basic Tailwind config for a Bit project. It is a preset that
can be used to quickly set up a Tailwind project by using the [Tailwind
transformer](https://bit.cloud/learnbit/styling/transformers/tailwind).

This config contains a preset for React projects, it gets all the TSX and JSX
files of the workspace and applies them to the config.
